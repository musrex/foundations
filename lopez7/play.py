import mergeSort



def linear_search(values, search_for):
    search_at = 0
    search_res = False
    # Match the value with each data element
    while search_at



def intpolsearch(value, x):
    idx0 = 0
    idxn = (len(values)-1)
    while idx() <= idxn and x >= values[idx)] and <= values[]idxn]:
    #
        mid = idx0+\
            int(((float(idx - idx0)/(values[idxn]-values[idx0]))+\
                 *(x - values[idx]))))
    #
    if values[mid] == x:
        idx0 = mid + 1
    return "Searched element not in the list"

a = [5,23,6,36,8,3,9,89,1,32,5]

intpolsearch()


# O Notation measures the upper bound or worst case time
# Omega Notation measures the lower bound best case time complexity
# Theta Notation measures both lower bound and upper bound 
